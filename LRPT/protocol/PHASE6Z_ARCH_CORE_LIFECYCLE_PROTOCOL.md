# PHASE 6.Z ARCH CORE LIFECYCLE PROTOCOL

timestamp_utc: 2026-02-13T10:47:01Z
scope: arch/system/ecosystem core lifecycle
mode: deterministic lifecycle command chain

## Activation Signature (verbatim)
p 6 z arch system ecosystem core lifecycle record restart reboot resync rebirth repulse rebit remove review

## Lifecycle Command Chain
1) `record`:
   - capture current state in maps/logs/snapshot.
2) `restart`:
   - apply restart semantics (`ssn/cld`) within active protocol contracts.
3) `reboot`:
   - perform controlled environment re-entry (facts-only governance context).
4) `resync`:
   - align protocol/version/date/status with SoT matrix.
5) `rebirth`:
   - regenerate phase baseline from validated evidence.
6) `repulse`:
   - reject unresolved drift tails from forward transition.
7) `rebit`:
   - apply minimal deterministic correction increments.
8) `remove`:
   - remove obsolete/non-canonical transition artifacts by policy decision.
9) `review`:
   - complete governance review and produce closure evidence.

## Gate Rules
- command chain execution order is mandatory.
- transition to next phase/gate requires chain completion with `COMPLIANT`.
- unresolved command stage => phase status `BLOCKED`.

## Dependencies
- `VERSION_GATE_RESET_PROTOCOL.md`
- `PHASE6C_ISOLATION_AUTONOMY_REVISION_PROTOCOL.md`
- `PHASE6D_BLACKHOLE_PROTOCOL.md`
- `PHASE70_SUNBIRTHLIGHTPULSEBIT_PROTOCOL.md`

## Constraints
- contracts-first
- observability-first
- derivation-only
- no runtime logic changes
