# DevKit Compatibility Facade

This directory is a canonical compatibility surface for `devkit/*` paths
referenced by governance contracts.

Implementation is delegated to `LRPT/*` artifacts to preserve existing runtime
and tests while removing path drift for operators and protocols.
